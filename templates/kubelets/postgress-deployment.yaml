apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  namespace: pathfinder
  name: {{ .Chart.Name }}-postgres
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-postgres
    spec:
      containers:
      - name: {{ .Chart.Name }}-postgres
        image: postgres:10.4
        imagePullPolicy: "Always"
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_DB
          value: database_pathfiner_official
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: database-pathfinder-official-secret
              key: password
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: database-pathfinder-official-secret
              key: user
